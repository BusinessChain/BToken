- Optimize GetProbeAtBlock such that the same blocks are not searched  multiple times when there are forks in the chain.
- In Blockdownload also download stale blocks, not only main chain.

- Network should not have the method consensusError. It should care only about its own network errors like Dos.
